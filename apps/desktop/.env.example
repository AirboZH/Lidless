# ── Tauri updater 签名密钥（仅本地打「签名包」时用）──────────────────────
#
# 用法：复制本文件为 .env 并填入真实值。.env 已被 gitignore，切勿提交。
# `pnpm desktop:build` 会通过 dotenvx 自动加载它；没有 .env 也能正常构建（只是不签名）。
#
# CI（GitHub Actions）不读本文件——它从 GitHub Secrets 注入同名变量。
#
# 密钥由以下命令生成（私钥存到仓库外，例如 ~/.tauri/lidless.key）：
#   pnpm desktop exec tauri signer generate -w ~/.tauri/lidless.key

# 私钥：可填「文件内容」或「文件路径」，Tauri 两者都支持。
# 例：TAURI_SIGNING_PRIVATE_KEY=~/.tauri/lidless.key
TAURI_SIGNING_PRIVATE_KEY=

# 生成密钥时设置的密码（生成时没设密码就留空）。
TAURI_SIGNING_PRIVATE_KEY_PASSWORD=
