{
  "name": "@lidless/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Keep awake while locked: prevent system sleep and keep the network online",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "dotenvx run --quiet --ignore=MISSING_ENV_FILE -- tauri build",
    "build:raw": "tauri build",
    "icon": "tauri icon ./app-icon.png"
  },
  "devDependencies": {
    "@dotenvx/dotenvx": "^1.39.0",
    "@tauri-apps/cli": "^2"
  }
}
